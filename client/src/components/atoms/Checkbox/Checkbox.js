import React/*, { Component }*/ from "react";
import { render } from 'react-dom';

const Checkbox = () => {
	return(
		
	)
}

render(<Checkbox />, document.querySelectorAll(""));

export default Checkbox;