import React/*, { Component }*/ from "react";
import { render } from 'react-dom';

const Settings = () => {
	return(
		<div>
			
		</div>
	)
}

render(<Settings />, document.querySelectorAll(""));

export default Settings;